import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Keep default config; Vercel will use this directory as the project root
  // when you set the "Root Directory" for the project to `sankey-finance`.
};

export default nextConfig;
